# Kalici
This 1. Open `InvestmentOps.xcodeproj` in Xcode. 2. Select target `InvestmentOps`. 3. Under Signing &amp; Capabilities:    - choose your Team    - update bundle ID if needed 4. Build and run on simulator or device.
